# Claude-Anweisungen für dieses Projekt

## Berechtigungen

Tests dürfen ohne Rückfrage ausgeführt werden, insbesondere:

- `docker compose exec -T app php artisan test ...`
- `npx playwright test ...`
